/*import { useLocation } from "@reach/router";
import React from "react";

export default ({}: { path?: string }) => {
  const location = useLocation();

  return (
    <div>
      <p>
        Sandbox will be here!
        <br />
        Location: {location.pathname}
      </p>
    </div>
  );
};
*/
