@plugin("@smeli/plugin-katex")

make_formula_card: expression => ui.layout {
  direction: "column"
  surface: "background"
  elevation: 2
  item0: katex.formula {
    code: katex.transpile(expression)
  }
}
